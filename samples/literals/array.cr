arr = [1, 2, 3 + 10]         # Array(Int32)
puts arr

[1, "hello", 'x'] # Array(Int32 | String | Char)

[] of Int32 # same as Array(Int32).new

%w(one two three) # ["one", "two", "three"]

%i(one two three) # [:one, :two, :three]
